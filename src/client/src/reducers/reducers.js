export function reducer(state, action) {
  switch (action.type) {
    case 'LOGIN_USER':
      return state
    case 'REGISTER_USER':
      return state
    case 'LOGOUT':
      return state
    case 'RECEIVE_CURRENT_USER':
      return state
    default:
      return state
  }
}
